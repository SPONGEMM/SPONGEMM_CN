---
import Footer from "../components/site/Footer.astro";
import Header from "../components/site/Header.astro";
import LegacyDocsLayout from "../components/legacy/LegacyDocsLayout.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { loadLegacyMarkdown } from "../lib/legacy-markdown";

const page = await loadLegacyMarkdown("教程");

if (!page) {
  throw new Error("Missing legacy tutorials overview page");
}

const Content = page.module.default;
---

<BaseLayout title="SPONGEMM | 教程">
  <Header />
  <main>
    <LegacyDocsLayout section="tutorials" currentPath="/tutorials" title={page.title}>
      <div class="legacy-markdown-body">
        <Content />
      </div>
    </LegacyDocsLayout>
  </main>
  <Footer />
</BaseLayout>
